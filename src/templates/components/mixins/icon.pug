//- @pugdoc
  name: icon
  description: Outputs an icon from the SVG stack.
  arguments:
    - {string} name - the name of the icon in the stack.
    - {number} width - the width of the icon.
    - {number} height - the height of the icon.

mixin icon(name, width, height)
  svg(width=width height=height aria-hidden="true")&attributes(attributes)
    use(href=`icons/stack.svg#${name}`)
